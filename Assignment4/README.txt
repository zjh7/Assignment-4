The only part of Assig 4 I could not figure out was the AFNetworking to preload HTML content.
If you click the google link, it is broken and will provide the alert view for an error loading HTML.
I used the HUD loading screen when loading HTML.
-Zack Hall
